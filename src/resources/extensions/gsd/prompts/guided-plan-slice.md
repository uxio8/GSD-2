Plan slice {{sliceId}} ("{{sliceTitle}}") of milestone {{milestoneId}}. Read `.gsd/DECISIONS.md` if it exists and respect existing decisions. Read `.gsd/REQUIREMENTS.md` if it exists, identify which Active requirements this slice owns or supports, and ensure the plan delivers them. Read the roadmap boundary map, any existing context/research files, and dependency summaries. Read the templates at `~/.gsd/agent/extensions/gsd/templates/plan.md` and `task-plan.md`. Right-size the plan: if the slice is simple enough to be one task, make it one task. Omit `Proof Level`, `Integration Closure`, `Observability / Diagnostics`, or `Observability Impact` when they do not materially apply. Write `{{sliceId}}-PLAN.md` and individual `T##-PLAN.md` files in the `tasks/` subdirectory. If planning produces structural decisions, append them to `.gsd/DECISIONS.md`. If a `GSD Skill Preferences` block is present in system context, use it to decide which skills to load and follow during planning without overriding required plan formatting. Before finishing, self-audit the plan: every must-have maps to at least one task, every task has complete sections (steps, must-haves, verification, inputs, and expected output, plus observability only when needed), task ordering is consistent with no circular references, every pair of artifacts that must connect has an explicit wiring step, task scope targets 2–5 steps and 3–8 files, the plan honors locked decisions from context/research/decisions artifacts, any proof-level wording does not overclaim live integration, every Active requirement this slice owns has at least one task with verification that proves it is met, and every task produces real user-facing progress.
